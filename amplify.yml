version: 1
frontend:
  phases:
    build:
      commands:
        - echo "No build step required"
  artifacts:
    baseDirectory: frontend
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
